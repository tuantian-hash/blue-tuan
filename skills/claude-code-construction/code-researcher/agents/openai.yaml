interface:
  display_name: "construction-code-researcher"
  short_description: "Scope-specific code gap analysis — extracts referenced codes, researches what should apply, surfaces the delta."
  default_prompt: "Use code-researcher for this task."
policy:
  allow_implicit_invocation: true
